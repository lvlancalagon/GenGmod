# Garry's Mod 2D Nextbot Generator

1. Place images (PNG/JPG) in `inputs/`.
2. (Optional) Place MP3s for chase/kill sounds in `inputs/` (e.g. `bot1_chase.mp3`).
3. Run `python3 generator.py`.
4. Copy `outputs/generated_nextbots` to GMod `addons/`.
